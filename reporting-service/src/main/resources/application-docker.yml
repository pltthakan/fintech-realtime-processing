spring:
  data:
    mongodb:
      uri: ${MONGO_URI}

eureka:
  instance:
    hostname: reporting-service
    prefer-ip-address: true

logging:
  level:
    root: INFO
    com.fintech.reporting: INFO
